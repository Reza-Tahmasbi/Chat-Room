import re
# take the length
message = "Private message, length=<23> from <rezatb> to <user_name1>,<user_name2>,<user_name3>:\n<message_body>"
# match = re.search(pattern, message)
# print(match.group(1))

# take the user_name
# pattern = r'from (.+) to'
# match = re.search(pattern, message)
